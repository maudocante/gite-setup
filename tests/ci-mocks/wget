#!/bin/bash

echo "[mock] wget" "$@" >&2
outfile=""
url=""
while [[ $# -gt 0 ]]; do
  case "$1" in
    -q) shift ;;
    -O) outfile="$2"; shift 2 ;;
    *) url="$1"; shift ;;
  esac
done
if [ -n "$outfile" ]; then
  echo "# mock content for $url" > "$outfile" || true
fi
exit 0
