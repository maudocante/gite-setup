#!/bin/bash

echo "[mock] gpg" "$@" >&2
out=""
dearmor=0
while [[ $# -gt 0 ]]; do
  case "$1" in
    --dearmor) dearmor=1; shift ;;
    -o) out="$2"; shift 2 ;;
    *) shift ;;
  esac
done
if [ "$dearmor" -eq 1 ] && [ -n "$out" ]; then
  cat - > "$out" || true
fi
exit 0
